# 进度记录（2026-08-17）

> 本文件记录 CodeScribe 改造进度，供跨会话恢复。最新状态以 git 提交与 `docs/03-执行计划.md` 为准。

## 当前状态：全部 12 项任务完成，MVP 可商用（无 npm 账号，CLI 走 GitHub Releases 分发）

| 阶段 | 任务 | 状态 | 提交 |
|---|---|---|---|
| Phase 1 | T1-T3 品牌改名 codesucker → codescribe | ✅ 完成 | `d4bbe60` |
| Phase 2 | T4-T6 类型抽象 + 渲染分发 + 元数据表单 | ✅ 完成 | `2b528c3` |
| Phase 2 | T7 用户手册（README 提取 + builder） | ✅ 完成 | `ab9c16b` |
| Phase 2 | T8 设计说明书 + 申请表 builder | ✅ 完成 | `9c1dec6` |
| Phase 3 | T9 @codescribe/cli 脚手架（scrub 命令） | ✅ 完成 | `7c341ed` |
| Phase 3 | T10 CLI 发布与文档 | ✅ 完成（走 GitHub Releases） | 待提交 |
| Phase 4 | T11 Pro 功能门控 | ✅ 完成 | `e2e5c98` |
| Phase 4 | T12 付费上线 | ✅ 完成 | `a4b5d47` |
| Phase 4 | 端到端走查（4 种文档真实导出） | ✅ 完成 | `80140de` |
| 收费 | 激活码签发工具 `issue-code` | ✅ 完成 | `e01f702` |

## 关键决策（已确认）
- 品牌：码著 CodeScribe，repo `codescribe`，定位「代码不出本机，文书一应俱全」
- 版本：`0.5.0-alpha`；appId `com.qson.codescribe`；包名 `@codescribe/{core,app}`
- 策略：单人开发、文档族优先、安检门当功能（免费 CLI 脱敏）、不做营销只靠 Inbound
- 付费：先一次性买断（99-299 元/次），后订阅；appId `com.qson.codescribe`
- 保留版权：Apache-2.0 + NOTICE + 原作者 fanbuz 署名（README 有演进声明）

## Phase 2 成果
- **4 种 DocumentType**：source-program / user-manual / design-spec / application-form，全部可生成 docx
- **渲染注册表**：`render-registry.ts` 按类型分发，源程序输出与 v0.4.4 字节级兼容
- **元数据**：Step3 文档类型卡片 + 10 字段元数据表单，持久化 `.codescribe.json`
- **core 测试**：11 个套件全绿（新增 doc-type / render-registry / user-manual / documents）
- `npm run verify` 全绿（版本/锁文件/图标/许可证/测试/构建/集成）

## Phase 3 成果
- **`packages/cli`**：bin `codescribe`/`codescribe-cli`，`scrub` 命令（文件/stdin → 脱敏 → stdout + 统计），复用 core 清洗状态机，字符串内 `//` 不误删；选项 `--keep-comments/--no-mask/--keep-blank`；esbuild bundle 10.6KB；core 加 `exports["./clean"]` 子路径
- **分发**：无 npm 账号，改走 GitHub Releases —— 构建产出便携版 `dist/codescribe-cli.mjs`（自包含，`node codescribe-cli.mjs scrub <file>` 即可用）；`release.yml` 新增 CLI 矩阵（构建 + 冒烟测试 grep 脱敏 + 上传资产）
- **测试**：`scrub.test.ts` 5 项断言；根 verify 已接入 CLI 的测试与构建

## Phase 4 成果（Task 11 Pro 门控）
- **激活码引擎** `main/license.ts`：`CS.<payload>.<sig>`（点分隔，避免 base64url `-` 冲突），HMAC-SHA256 签名最小防御；`issueCode/validateCode/activate/deactivate/getLicenseStatus/statusOf/isProDocType`；持久化 `userData/license.json`
- **IPC**：`license:status/activate/deactivate`（`license-ipc.ts`）；preload `cs.licenseStatus/Activate/Deactivate`；store `license/licenseOpen` + `refreshLicense/isPro/docTypeNeedsPro`
- **UI 门控**：Step3 Pro 类型卡片「PRO 已锁定」+ 激活弹窗；Step5 导出按钮锁定「激活 Pro 后导出」；`pipeline.ts` 导出 handler 纵深防御校验
- **测试**：`test/license.ts` 全过（签发/校验/篡改/到期/永久/门控/停用）

## Phase 4 成果（Task 12 付费上线）
- **共享激活弹窗** `screens/LicenseModal.tsx`：Step3 与 Settings 共用，含定价对比卡（免费版 ¥0 / Pro 一次性买断 ¥99–299·永久，按源码行数阶梯）
- **购买流程**：微信/支付宝人工发码 → 备注「码著 Pro + 邮箱」→ 官方发码 → 粘贴激活；服务商/批量采购入口
- **Settings 关于区**：显示授权状态（免费版/Pro 已激活）+ 升级/管理 Pro 入口

## 待办事项
1. **收款渠道落地**：正式微信/支付宝收款码或收款链接，用于发码流程
2. **人工走查（GUI）**：跑 `npm run dev` 走完 5 步向导，确认 Word 排版与激活三态 UI
3. **首次发版**：打 `v0.5.0-alpha.*` tag 触发 `release.yml`，产出 dmg/exe + CLI 资产并生成 GitHub Release
4. **可选**：注册 npm 账号后补 `npm publish -w @codescribe/cli`

## 发码与验证
```bash
# 签发限期码（如 2027-12-31 到期）
npm run issue-code -w @codescribe/app -- "客户名" 2027-12-31
# 签发永久码
npm run issue-code -w @codescribe/app -- "客户名"
```
输出：激活码 + 被许可人/到期日/功能位 + 自校验。买方在 App「升级 Pro」粘贴激活即可。

## 环境
- 仓库：`/Users/son/Desktop/AHEAD/private/other/tool/codescribe`
- remote：origin `git@github.com:Qson8/codescribe.git`；upstream `https://github.com/fanbuz/codesucker.git`
- Node v22.23.2；`npm run verify` 为发布前完整校验